[package]
name = "gpui_util"
version = "0.1.0"
rust-version.workspace = true
repository.workspace = true
description = "Utility types used by the standalone GPUI framework."
readme = "README.md"
license = "Apache-2.0"
publish = true
edition.workspace = true

[dependencies]
log.workspace = true
anyhow.workspace = true

[target.'cfg(target_os = "windows")'.dependencies]
which.workspace = true

[lints]
workspace = true
